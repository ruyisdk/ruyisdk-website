import ReleaseProvider, { DownloadRuyi } from '@site/src/components/docs_utils/LatestReleases';

<ReleaseProvider>
  <>

:::tip
Durch das Herunterladen und Verwenden von RuyiSDK stimmen Sie den Lizenzbedingungen und der [Datenschutzerklärung](https://ruyisdk.org/docs/legal/privacyPolicy/) zu.
:::

## Ruyi herunterladen

Ruyi bietet vorgefertigte Binärdateien für die Architekturen x86_64, riscv64 und aarch64 an. Führen Sie ``uname -m`` aus, um die Systemarchitektur zu überprüfen, und laden Sie dann die entsprechende Binärdatei herunter.

Für ``x86_64``：

<DownloadRuyi arch="x86_64" />

Für ``aarch64``：

<DownloadRuyi arch="aarch64" />

Für ``riscv64``：

<DownloadRuyi arch="riscv64" />

:::tip
Falls die Seite den Download-Link nicht korrekt anzeigt, können Sie das [RuyiSDK-Mirror](https://mirror.iscas.ac.cn/ruyisdk/ruyi/releases/) besuchen, um die neuesten Binärdateien herunterzuladen. Alternativ finden Sie sie auch bei den [GitHub-Releases](https://github.com/ruyisdk/ruyi/releases).
:::

## Ruyi installieren

Nehmen Sie die Binärdatei für x86_64 als Beispiel. Fügen Sie der Datei Ausführungsrechte hinzu:

```bash
$ chmod +x ./ruyi.amd64
```

Verschieben Sie die Binärdatei in ein Verzeichnis, das im PATH-Systempfad enthalten ist:

```bash
$ sudo cp -v ruyi.amd64 /usr/local/bin/ruyi
```

:::tip
Stellen Sie sicher, dass der Dateiname ``ruyi`` lautet.
:::

Führen Sie den folgenden Befehl aus, um die Installation zu überprüfen:

```bash
$ ruyi version
```

Wenn der Befehl die Versions- und Copyrightinformationen ausgibt, wurde Ruyi erfolgreich installiert. Falls der Befehl fehlschlägt, überprüfen Sie Ihre Systemumgebung und wiederholen Sie die Schritte.

## Ruyi verwenden

Lesen Sie die Dokumentation, um zu erfahren, wie Sie Ruyi effektiv nutzen können.

  </>
</ReleaseProvider>

